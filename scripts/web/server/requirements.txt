# ECOMAP Web UI backend - pinned dependencies (Python 3.12)
# ASCII only: pip decodes this file with the system locale codec (gbk on
# zh-CN Windows), so non-ASCII bytes here abort the install.
fastapi==0.115.*
uvicorn[standard]==0.30.*
matlabengine==24.2.*
python-multipart==0.0.32
jinja2==3.1.*
pytest==9.*
# TestClient (starlette) transport dependency; needed to run tests/test_api.py
httpx==0.28.*
