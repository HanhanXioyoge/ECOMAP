[pytest]
testpaths = tests/python tests/integration tests/regression
python_files = test_*.py
addopts = -m "not slow"
markers =
    slow: end-to-end tests that exercise the full HTTP stack (skip by default)